# 2D SLAM 從零寫：可行性 spike

2026-09-18 · 研究 session。Paul 的題目：「從零寫一個 SLAM 的最小版本（2D）— 讀者用方向鍵開一台小車，看地圖與位姿同時被估出來、閉環時整張圖『啪』地修正。」Lite3 那篇的續集，也對得上 SyncAI-Dog。

分支 `feat/slam`，worktree `/Users/paul_jiang/Desktop/Paul/Blog-slam`，只有本機 commit。程式在 `content/posts/slam-2d/components/`，測試在 `tests/ml/slam-2d.test.ts`。還沒有任何 UI。

## 結論

**可行，而且「啪」是真的。** 純 TypeScript、沒有任何相依套件，大約 350 行：模擬的光達、point-to-line ICP、pose graph 加 Gauss–Newton。在模擬裡繞環形走廊兩圈：

- 只靠里程計：平均誤差 2–2.8 m，終點偏 5 m 以上。
- 加上閉環：平均誤差 0.02–0.27 m，終點 0.01–0.03 m。
- **第一次閉環的那一步**，最新位姿的誤差從 2.2–2.8 m 掉到 0.00–0.05 m，整圈的平均誤差從 1.0–1.9 m 掉到 0.16–0.41 m。
- 三個 seed、約 300 次閉環，**沒有一次接錯**（用真值檢查）。

## 設計

| 部件 | 做法 | 檔案 |
| --- | --- | --- |
| 位姿 | SE(2)：`compose`、`inverse`、`between` | `se2.ts` |
| 世界與光達 | 線段組成的環形走廊（20×14 m，中間一塊實心），180 條光束、8 m、測距雜訊 σ = 2 cm | `world.ts` |
| 里程計 | 每步的前進量有 2% 的雜訊，加上一個**固定的轉向偏差**（預設 0.006 rad/m，像左右輪徑不一樣）；固定偏差才會累積成漂亮的彎曲軌跡 | 測試裡的 `drive` |
| 掃描比對 | **point-to-line ICP**：最近點加上它在掃描順序裡的鄰居當成一小段牆，解 3×3 的最小平方；同時回傳資訊矩陣 | `icp.ts` |
| 後端 | pose graph（Grisetti et al. 2010）。邊帶完整的 3×3 資訊矩陣；Gauss–Newton，稠密 Cholesky，第 0 個節點釘住 | `graph.ts` |
| SLAM 迴圈 | 每 0.5 m 或 0.35 rad 一個 keyframe；里程計邊；對「估計距離 < 2.5 m、且相隔 ≥ 25 個 keyframe」的舊 keyframe 試 ICP，過關就加閉環邊並重新最佳化 | `slam.ts` |

## 量到的（`SLAM_BENCH=1 pnpm vitest run tests/ml/slam-2d.test.ts -t bench --silent=false --reporter=verbose`）

兩圈，約 200 個 keyframe，三個 seed：

| 設定 | 平均誤差 | 最大 | 終點 | 閉環數（接錯） | 最慢的一步 |
| --- | --- | --- | --- | --- | --- |
| 只靠里程計 | 1.96–2.82 m | 5.2–5.6 m | 4.95–5.48 m | 0 | 2 ms |
| 相鄰掃描比對修里程計，不閉環 | 0.89–0.95 m | 2.2–2.3 m | 2.2–2.3 m | 0 | 4 ms |
| 閉環 | 0.02–0.27 m | 0.06–0.47 m | 0.01–0.03 m | 93–104（0） | 85 ms |
| 兩者都開 | 0.02–0.03 m | 0.04–0.07 m | 0.00–0.01 m | 102–104（0） | 85 ms |

第一次閉環發生在第 99–108 個 keyframe（剛好繞完一圈）：

| seed | 整圈平均誤差 | 最新位姿的誤差 |
| --- | --- | --- |
| 1 | 1.52 → 0.16 m | 2.66 → 0.00 m |
| 2 | 1.02 → 0.26 m | 2.21 → 0.01 m |
| 3 | 1.88 → 0.41 m | 2.80 → 0.05 m |

里程計偏差多大還救得回來（seed 1）：

| 轉向偏差 | 只靠里程計的終點誤差 | 閉環後的平均誤差 |
| --- | --- | --- |
| 0.003 rad/m | 2.4 m | 0.09 m |
| 0.006（預設） | 4.9 m | 0.10 m |
| 0.012 | 9.4 m | 1.32 m，閉環變少（89 次），第一次閉環比較晚，終點仍偏 0.5 m |
| 0.02 | 13.6 m | **一次閉環都沒有**：估計的位置離真正的舊位置超過 2.5 m，系統根本不知道自己回來了 |

## 三個值得寫進文章的發現

1. **point-to-point ICP 不夠用。** 第一版用最近點對最近點：就算初始猜測完全正確，殘差也有 5–6 cm（180 條光束打在牆上的點本來就不是同一批點），好的配對和壞的配對分不出來，結果一次閉環都沒有。換成 point-to-line 之後，正確配對的殘差是 2.2 cm，剛好等於感測器雜訊，門檻才有意義。
2. **走廊問題（aperture problem）。** 站在走廊中段，初始猜測沿走廊方向偏 1–3 m，ICP 會很有信心地收斂到錯的答案（內點 0.71–0.80、殘差 3 cm）：兩面平行的牆只告訴你離牆多遠，沒有告訴你沿著牆走了多遠。在轉角處，偏 3 m、0.6 rad 都收斂得回來。解法有兩個，都用上了：
   - ICP 的 Hessian 就是資訊矩陣。位置部分的兩個特徵值之比（`conditioning`）在轉角是 0.90，在走廊正確配對是 0.25，在走廊錯的配對是 0.03–0.06。低於 0.15 的閉環直接不要。
   - 留下來的閉環，把整個 3×3 資訊矩陣放進 pose graph 的邊，所以「沿走廊方向不確定」這件事會如實傳進最佳化。
3. **找閉環靠的是自己的位置估計，所以漂太遠就找不回來。** 偏差 0.02 rad/m 時系統完全失效。真的系統（Cartographer，Hess et al. 2016）用 branch-and-bound 在一大片範圍裡搜尋；這是「最小版本」和真實系統最大的差距，適合當文末的一節，也適合做成一個讓讀者把漂移調大、看它失效的滑桿。

## 瀏覽器預算

- 每個 keyframe 最慢 85 ms（200 個 keyframe 時）。時間花在兩處：候選 ICP（每次 180×180 的暴力最近鄰 × 最多 30 次迭代 × 候選數）和稠密 Cholesky（600×600）。
- **稠密 Cholesky 是 O(n³)**：500 個 keyframe 時約 11 億次運算，會卡住一秒左右。文章裡的地圖如果要開很久，得限制 keyframe 數、或改成只在閉環時最佳化、或利用稀疏性。目前每次閉環都重解，而第二圈幾乎每個 keyframe 都閉環，這也是浪費。
- 這些數字是 Node 裡量的，還沒有在瀏覽器裡跑過；也還沒有畫佔據格地圖的成本。

## 還沒做的

- 沒有 UI、沒有佔據格地圖（目前「地圖」就是各 keyframe 的掃描點）。
- 沒有測讀者亂開車的情況（原地打轉、倒車、貼牆），只測了固定路線。
- 沒有測錯誤閉環的後果，也沒有做穩健核函數；目前靠門檻擋，300 次裡 0 次錯，但只有一張地圖。
- 真實光達的問題（動態物體、玻璃、運動畸變）完全沒碰。
- SyncAI-Dog 用的是哪一種 SLAM 我不知道，要問 Paul，文章才能接上去。

## 建議的文章骨架

1. 開場儀器：方向鍵開車。左邊是「真相」，右邊是車子自己以為的地圖，一開始就歪；繞回起點的那一刻，整張圖啪地彈正。
2. 車子知道什麼：輪子轉了幾圈（會漂）、周圍一圈的距離（很準，但只有當下）。
3. 只靠輪子會怎樣：誤差怎麼累積。互動：調偏差，看軌跡怎麼彎。
4. 兩張掃描怎麼對起來：ICP，一步一步的動畫。互動：自己拖一張掃描去對另一張，再按「讓 ICP 來」。
5. 走廊問題：為什麼在走廊裡對不準，怎麼從數學上看出「這個方向我不確定」。
6. 把所有的「我覺得」放進一張圖：pose graph 是一堆彈簧。閉環是一根新的彈簧，把整條鏈子拉回來。互動：手動加一條閉環邊，看鏈子怎麼鬆開。
7. 它會在哪裡失敗：漂太遠找不到閉環；接錯一條邊整張圖就毀了。
8. 和真的系統差在哪：Cartographer 的子地圖與 branch-and-bound、穩健核、3D、視覺 SLAM。

## 參考資料（查過存在）

- Grisetti, Kümmerle, Stachniss, Burgard, *A Tutorial on Graph-Based SLAM*, IEEE ITS Magazine 2(4):31–43, 2010, doi:10.1109/MITS.2010.939925
- Hess, Kohler, Rapp, Andor, *Real-Time Loop Closure in 2D LIDAR SLAM*, ICRA 2016, doi:10.1109/ICRA.2016.7487258（Cartographer）
- 還沒查證、憑記憶：Censi 2008 的 PLICP（point-to-line ICP 的出處）、Besl & McKay 1992（ICP）、Lu & Milios 1997（最早的 pose graph）

## 補充（2026-09-19）：SyncAI-Dog 用的是什麼

Paul 回覆：**3D 光達 + 視覺**。所以文章結尾「和真的系統差在哪」要接的是這兩條線，而不是 2D 的 Cartographer：

- 3D 光達：掃描是點雲而不是一圈距離，位姿是六個自由度；掃描比對的想法相同（ICP 的各種變形），但要處理運動畸變，通常和 IMU 緊耦合。這一類的代表作我記得是 LOAM 系列和 FAST-LIO，**還沒查證**，寫進文章前要確認出處。
- 視覺：用影像特徵而不是距離；閉環靠外觀辨識（這剛好補上本文最小版本最大的弱點：靠自己的位置估計去找舊地方，漂太遠就找不回來）。
- 共通的部分就是本文的後半：不管前端是什麼感測器，後端都是一張位姿圖加最小平方。這是文章可以誠實說「你剛剛寫的那一半，和機器狗身上的是同一個東西」的地方。

## 補充（2026-09-19）：接錯一條閉環會怎樣

健康的兩圈（104 條正確的閉環）：平均誤差 0.10 m、最大 0.17 m。再硬加**一條**錯的閉環（宣稱下方走廊的第 20 個 keyframe 和上方走廊的第 70 個是同一個地方、方向相反，這正是對稱的建築會誘發的錯誤）：

| 情況 | 平均誤差 | 最大誤差 |
| --- | --- | --- |
| 104 條對的 | 0.10 m | 0.17 m |
| 104 條對的 + 1 條錯的 | 3.92 m | 8.91 m |
| 同上，錯的那條權重 ×10 | 3.93 m | 8.92 m |
| 第一圈只有里程計（1.48 m）+ 只有那 1 條錯的 | 5.25 m | 11.86 m |

104 比 1 也救不回來：最小平方對離群值沒有抵抗力，而且一條差了 180° 的邊會把 Gauss–Newton 帶進另一個局部最小值。這是「為什麼真的系統寧可漏掉十個閉環，也不要接錯一個」的直接證據，也是穩健核函數存在的理由（這裡沒有實作）。

## 正式儀器的設計提案（等 Paul 確認後才動工）

原則沿用前幾篇：讀者一進來就能玩；每個儀器只講一件事；全部是量過會成立的效果。閱讀量控制在 10 分鐘內。

| # | 儀器 | 讀者做什麼 | 看到什麼（都量過） |
| --- | --- | --- | --- |
| 01 | 開車（開場） | 方向鍵／觸控鈕開車，或按自動駕駛 | 左：真實世界。右：車子自己的地圖慢慢歪；回到起點那一刻整張圖彈正，誤差約 2.7 m → 0.01 m |
| 02 | 只靠輪子 | 調「輪子不準」的滑桿，放車跑一圈 | 同一條路，里程計畫出來的軌跡怎麼彎；終點差 2.4 / 4.9 / 9.4 m |
| 03 | 把兩張掃描對起來 | 用手拖一張掃描去對另一張，再按「交給 ICP」；切換「轉角／走廊」 | ICP 一步步收斂的動畫。轉角：偏 3 m 也對得回來。走廊：會很有信心地對錯；同時畫出「不確定橢圓」，走廊裡它沿走廊拉得很長 |
| 04 | 彈簧 | 一條歪掉的位姿鏈；按「加上閉環」 | 每一輪 Gauss–Newton 後鏈子怎麼鬆開、誤差數字怎麼掉；閉環邊就是一根新彈簧 |
| 05 | 弄壞它 | 三個開關：關掉閉環、把漂移拉到 0.02 以上、接錯一條閉環 | 地圖一路歪下去；永遠認不出自己回來了；一條錯的邊把 0.10 m 的地圖毀成 3.9 m |

實作備註：01 和 05 是同一個元件（05 多三個開關）；02 重用車子與里程計；03、04 各自獨立、很小。不需要任何外部套件，頁面不需要「按下才載入」。手機上 01 要有虛擬方向鍵（臨時版已有）。

### 文章骨架（對應儀器）

1. 開場 + 儀器 01：先看到「啪」。一句話講清楚問題：沒有 GPS、沒有地圖，車子要一邊畫地圖一邊知道自己在哪，而這兩件事互相需要對方。
2. 車子只有兩種感覺：輪子（一直有，但會漂）和一圈測距（很準，但只有當下）。儀器 02。
3. 把兩張掃描對起來，就知道自己動了多少。儀器 03，順便講走廊問題。
4. 所有的「我覺得」放在一起：位姿圖與彈簧。儀器 04。這一節解釋「啪」。
5. 它會怎麼壞。儀器 05。
6. 和機器狗身上的差在哪：3D 光達、視覺、IMU；後端其實是同一個東西。

## 補充（2026-09-19）：給小車一台相機，用「這個畫面我看過」找閉環

Paul 的決定：第 6 節「和機器狗身上的差在哪」愈深愈好，但每一步都要能在網頁上執行。SyncAI-Dog 是 3D 光達 + 視覺，所以先做視覺這一條，因為它剛好補上最小版本最大的弱點。

做法（`world.ts` 的 `paint`／`panorama`、`place.ts`、`slam.ts` 的 `candidates: "appearance"`）：

- 每面牆漆上自己的條紋（寬度 1.5–4 m、亮度與對比各不同）。光達的每一條光束順便回報打到的牆面亮度，得到一張 1×180 的環景影像。
- 兩張環景在同一個地點、不同朝向拍的，就是同一張圖左右平移。所以比對時把每一種平移都試一次：最好的相關係數 = 兩個地方有多像，最好的平移量 = 朝向差多少。比對前先做 7 格的模糊，因為細節會隨著每一步移動而改變，大輪廓不會。
- 像的才拿去做 ICP，初始猜測是「位置相同、朝向由平移量給」。ICP 的三道門檻（內點、殘差、conditioning）照舊，所以外觀只負責「提名」，幾何負責「確認」。

先量描述子本身（60 個隨機地點）：

| | 相關係數 | 朝向誤差 |
| --- | --- | --- |
| 同一地點、相距 0.25 m | p10 0.93，中位數 0.97 | 中位數 0.01 rad |
| 同一地點、相距 0.5 m | p10 0.79，中位數 0.91 | 中位數 0.01 rad |
| 同一地點、相距 1 m | p10 0.59，中位數 0.73 | 中位數 0.03 rad |
| 不同地點（> 4 m，1,499 對） | 中位數 0.45，p99 0.72，最大 0.82 | – |

第一版條紋太細（0.4–1.8 m）而且沒有模糊，相距 0.5 m 的同一地點 p10 只有 0.42，和不同地點的 p99（0.52）重疊，不能用。門檻定在 0.8。

再量整個系統（兩圈、3 個 seed）：

| 里程計偏差 | 靠位置估計找候選 | 靠外觀找候選 |
| --- | --- | --- |
| 0.006 rad/m | 103 次閉環、0 錯、終點 0.00–0.01 m | 100–102 次、0 錯、終點 0.01 m |
| 0.02 | **0 次閉環**，終點 13.0–14.0 m | 100–102 次、0 錯、終點 0.01 m |
| 0.03 | **0 次閉環**，終點 15.2–15.9 m | 100–102 次、0 錯、終點 0.01 m |

也就是說：靠自己的位置去找舊地方，漂太遠就永遠找不回來；靠「長得像」去找，漂多遠都沒差。這就是視覺在真的系統裡最重要的用途之一，而且它在這裡只花了約 40 行。每個 keyframe 最慢仍是約 90 ms。

還沒做的：環境裡有長得一樣的地方（perceptual aliasing）時會怎樣。現在每面牆的漆都不同，所以沒有這個問題；真的建築有。ICP 的門檻會擋掉一部分，但這件事沒有量。

## 補充（2026-09-19）：3D 光達在瀏覽器裡跑得動嗎

`content/posts/slam-2d/components/lidar3d/icp3.ts`，bench：`LIDAR3D_BENCH=1 pnpm vitest run tests/ml/lidar3d.test.ts -t bench --silent=false --reporter=verbose`。

- 世界：一個 16×12×4 m 的房間加 7 個箱子（AABB）。光達：16 線、垂直 ±15°、水平 1° 一條，一圈 **5,760 個點**，測距雜訊 2 cm。產生一圈 1–2 ms。
- 法向量直接從掃描的格狀結構算（同一線上的鄰居 × 上一線的鄰居），這也是真的光達管線的做法。
- point-to-plane ICP，六個未知數（三個平移、三個旋轉），最近鄰用 0.6 m 的體素格。

| 來源點取樣 | ICP 時間 | 誤差（12 組隨機 6-DoF 運動：±0.4 m、偏航 ±0.15 rad、俯仰／翻滾 ±0.04 rad） |
| --- | --- | --- |
| 全部 | 100 ms | 0.4 cm，0.14° |
| 每 3 點取 1 | 28 ms | 0.7 cm，0.24° |
| 每 6 點取 1 | 17 ms | 1.2 cm，0.18° |

光達里程計（只把相鄰兩圈的 ICP 串起來，完全沒有閉環）：31 m、120 圈，漂移 **0.67 m、6.5°**，每圈 39 ms。

結論：**可行**。一圈 28–39 ms，以每秒 5–10 圈的速度即時跑沒有問題，用 Three.js（站上已經有）畫點雲。漂移會累積，所以閉環與位姿圖在 3D 裡一樣需要，這正是文章想說的「後端是同一個東西」。

沒做的：SE(3) 的位姿圖（要把 `graph.ts` 從 3 個自由度改成 6 個，稠密解法的成本變 8 倍）、運動畸變（真的光達一圈要 0.1 秒，車子在動）、IMU。第 6 節建議的深度：3D 光達做到「里程計 + 看得到漂移」，閉環仍然用 2D 那一套解釋，不另外寫 SE(3) 的後端。

## 補充（2026-09-19）：「自動駕駛會卡住」的原因與修正

Paul 回報自動駕駛時會卡住。在瀏覽器裡量每一格畫面的間隔（每 10 秒一組），原因有兩個，互相獨立：

| 自動駕駛經過 | p95 畫面間隔 | 超過 100 ms 的畫面數 | 閉環數 | 備註 |
| --- | --- | --- | --- | --- |
| 10–60 秒 | 17–50 ms | 0 | 0 → 80 | 第一圈很順 |
| 70–80 秒 | 83–100 ms | 2 → 14 | 109 → 136 | 開始頓 |
| 90–110 秒 | 167–250 ms | 20–24 | 159 → 201 | 每個 keyframe 都頓一下，實際只剩 4–5 fps |
| 120 秒以後 | 33 ms | 0 | 209 | **車子完全不動**：到了 320 個 keyframe 的上限 |

1. **越跑越頓。** 第二圈起幾乎每個 keyframe 都找得到舊地方，每次都要 (a) 對半徑內所有舊 keyframe 做 ICP（第二圈 10 個、第三圈 18–19 個）、(b) 重解整張位姿圖。稠密 Cholesky 是 O(n³)：一輪在 100 / 200 / 300 個 keyframe 時是 10 / 26 / 87 ms，而且每次最多 8 輪。300 個 keyframe 時一步要 183 ms。
2. **到上限就靜靜停住。** `MAX_KEYFRAMES = 320` 時主迴圈連車子都不再前進，只在一行小字裡說「地圖畫滿了」，看起來就是當掉。

修正（`slam.ts`、`graph.ts`、`drive-lab.tsx`）：

- 認出一次舊地方之後休息 4 個 keyframe 再找（`restAfterClosure`）：相鄰的閉環說的幾乎是同一件事。
- 先用便宜的方式排序候選（距離或外觀分數），只對最好的 3 個做 ICP（`maxCandidates`）。
- 最佳化最多 4 輪，某一輪的改善小於萬分之一就停（原本的門檻 1e-9 形同沒有）。
- 到上限時：關掉自動駕駛，在畫面正中央說明原因並給「重來」按鈕。

修正後，同樣的量測：p95 全程 33–50 ms；超過 100 ms 的畫面在 80–100 秒之間共 14 格（原本每 10 秒 20 格以上），最大 200 ms。準確度沒有變差（兩圈、3 個 seed）：閉環從約 103 次變成 21 次，0 次錯，終點誤差 0.01–0.02 m，平均 0.07–0.21 m；偏差 0.02 rad/m 時靠外觀仍然 21 次、0 錯、終點 0.01 m。

還沒解決的：稠密解法本身沒變，所以 300 個 keyframe 附近每 1.5 秒左右仍有一次約 0.1–0.2 秒的頓挫。要根治得用稀疏 Cholesky，或把最佳化搬到 Web Worker。另外畫面更新率會隨地圖變大而下降（地圖有五萬多根線段，每次 keyframe 重建一次）；這在軟體繪圖的 headless 裡很明顯，真的 GPU 上沒有量。

## 補充（2026-09-19）：自動駕駛「撞牆卡住」的原因與新的駕駛

Paul 回報修完效能之後，自動駕駛仍然會撞牆卡住。這是另一個問題，出在駕駛本身。

**原因。** 第一版的自動駕駛只知道四個角落，永遠朝「下一個角落」直線開，而且「下一個是哪個」是一個和車子位置無關的計數器。從起跑線開始時，這條直線剛好在走廊裡，所以我所有的測試都沒發現問題（240 秒、28 個角落，從不卡住）。但讀者只要先自己開一段、再按自動駕駛，那條直線多半穿過中間那一大塊牆；車子頂著牆、油門踩著、方向對著牆後面的目標，就永遠不動了。

量測（`tests/ml/autopilot.test.ts`）：在走廊裡隨機放 200 個位置與朝向再啟動自動駕駛，舊版 **127 / 200 卡死**（連續兩秒不動）。

**新的駕駛**（`car.ts` 的 `autopilot`），三層：

1. 路線是走廊中線上每公尺一個點的環。每一步找離車子最近的那一點，瞄準它前面 3 m 的點（pure pursuit）。目標永遠由車子現在的位置決定，沒有計數器。
2. 看光達：正前方 ±25° 內 1 m 以內有東西，就減速並往比較空的那一側打方向，越近打越多；0.45 m 以內就停下來原地轉。
3. 脫困：油門踩著卻 0.4 秒沒動，就倒車 0.9 秒並往空的那一側甩尾，再繼續。

它用的是車子的真實位置（它是「駕駛」，不是被測試的機器人），但避障用的是和機器人相同的那一圈測距。

結果：同樣 200 個隨機起點，**0 / 200 失敗**（90 秒內至少繞一圈，且沒有一次停超過 3 秒）；從起跑線 240 秒繞 5 圈以上，最長停頓小於 1.5 秒。第一次跑出 4 個失敗，查出來是測試把車子放進了走廊邊的空心箱子裡，讀者開不進去，屬於測試的錯。

瀏覽器裡重現 Paul 的情境（手動轉向、頂著牆開 2.5 秒，再按自動駕駛）：它自己倒車脫困、開始繞圈，100 秒內認出舊地方 39 次，位置誤差 0.10 m，console 沒有錯誤。原地轉向會多記一些 keyframe，所以 320 筆的上限大約在 100 秒時到。

連帶的改動：儀器 02 和 04 重播的固定路線改用同一個駕駛（`driveLaps`），數字跟著變：02 預設設定兩圈後差 4.3 m（原本 5.3）；04 閉環前最歪的一點差 3.10 m，閉環後 0.17 m。
