#!/bin/zsh
# Run 8: disturbances training never showed (shove, a second block, pixel noise, dim light), on the two aux recipes at 10 000 steps.
cd /Users/paul_jiang/Desktop/Paul/Blog-vla
OUT=/Users/paul_jiang/Desktop/Paul/vla-work/run8
for kind in closed+shake open+shake; do for s in 11 12 13 14 15; do
  HC_MORE=1 HC_STEPS=10000 HC_AUX=1 HC_SEEDS=$s HC_TAG=-$s HC_ANALYTIC=0 HC_KINDS=$kind HC_OUT=$OUT pnpm exec vitest run tests/headcam/spike.test.ts --disable-console-intercept > $OUT/${kind/+/-}-$s.log 2>&1 &
done; done
wait
echo DONE
