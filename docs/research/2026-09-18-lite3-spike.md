# № 006 Lite3：可行性 spike

2026-09-18 · 研究 session（paul-d9）。選題見 [2026-09-18-article-006-topic.md](2026-09-18-article-006-topic.md)，Paul 已確認寫 Lite3。全部在 scratchpad 裡做，repo 只加了 `docs/research/` 底下的檔案，沒有裝任何套件到專案裡。

## 結論

**可行，而且沒有用到 onnxruntime。** 官方 policy 用手寫的 30 行 MLP 跑，配上 DeepMind 官方的 MuJoCo WASM，在 Node 裡走出來的軌跡和 Python MuJoCo + onnxruntime 一致到小數第二位，速度是即時的 29 倍。

## 量到的東西

腳本在 [lite3-spike/](lite3-spike/)（副檔名加了 `.txt`，避免被 lint／tsc 掃到）：`reference.py` 是 Python 參考實作，`rollout.mjs` 是 WASM 版，`sweep.mjs` 是擾動測試，`sweep-output.txt` 是它的完整輸出。

| 項目 | 結果 |
| --- | --- |
| policy 結構 | 已打開 ONNX 確認：`Gemm → Elu` ×3 再一層 `Gemm`，45→512→256→128→12，189,324 個參數，沒有 metadata |
| 手寫 MLP vs onnxruntime | 最大誤差 6.6e-6（float64 numpy）；JS float32 版對 50 筆參考觀測的最大誤差 1.3e-7 |
| 機器人模型 | **不在 deploy repo 裡**，在 submodule `DeepRoboticsLab/deep_robotics_model`（BSD-3，pin 在 `b452a1f`）：`Lite3/Lite3_mjcf/mjcf/Lite3.xml` + 4 個 STL，自帶地板。上一份筆記說的「2185 個 STL、9 MB」是樓梯地形，不是機器人，用不到 |
| 關節順序 | MJCF 的順序和 policy 的順序相同（FL, FR, HL, HR × HipX, HipY, Knee），不需要重排 |
| 控制迴圈 | 物理 1 kHz；每 12 步跑一次 policy（83 Hz）；τ = 30·(目標 − q) − 1·q̇，上限 ±30 N·m；目標 = 動作 × (0.125, 0.25, 0.25) + 預設姿勢 (0, −0.65, 1.3) |
| 觀測 45 維 | 角速度 ×0.25、機身座標下的重力方向、速度指令、關節角 − 預設、關節速度 ×0.05、上一次動作 |
| Python 參考 | 指令 vx 0.5 → 10 秒走 4.69 m（0.47 m/s），機身高度 0.32 m，沒倒 |
| WASM 版 | 同樣 10 秒：x 4.69、y −0.53、z 0.323，和 Python 相同；前 5 筆觀測的最大差 6.9e-8 |
| 速度（Node，M 系列） | 物理 21.6 µs/步，policy 149 µs/次 → 即時的 29 倍；一個 60 Hz 畫面要的模擬只花 0.57 ms |
| 啟動 | WASM 初始化 33 ms，編譯模型（含網格凸包）173 ms |

### 下載大小（gzip −9）

| 檔案 | 原始 | gzip |
| --- | --- | --- |
| `mujoco.wasm`（`@mujoco/mujoco` 3.13.0） | 10.2 MB | 2.49 MB |
| `mujoco.js` | 293 KB | 68 KB |
| 4 個 STL（98k 頂點） | 3.4 MB | 2.02 MB |
| policy 權重 float32 | 757 KB | 約 0.7 MB（ONNX 檔 gzip 後 705 KB；float16 可降到 379 KB 原始） |
| 合計 | | **約 5.3 MB** |

編譯成 `.mjb` 反而是 37.9 MB（gzip 18.6 MB），不要用。網格是最容易省的地方：外觀用減面後的網格，碰撞用膠囊或盒子，估計可以到 1 MB 以下，但改碰撞體會改變步態，要重新對一次參考軌跡。

## 哪些互動真的有戲

每個設定跑 10 秒、指令 vx 0.5，單次執行（物理是確定性的，雜訊那組用固定 seed）。

- **速度指令**：要 0.25 / 0.5 / 0.8 / 1.2 / 2.0，得到 0.21 / 0.47 / 0.82 / 1.26 / 2.04 m/s。官方 deploy 把指令上限設在 0.8，但 policy 到 2.0 都跟得上。側移和倒退也可以。直走會慢慢偏（10 秒偏 0.5–2 m），沒有人在修正航向，這本身可以講。
- **側推**（推 0.1 秒）：150 N 撐得住，200 N 倒。門檻很清楚，適合做成「你推推看」。
- **蒙住一種感覺**（把那組觀測歸零）：
  - 陀螺儀：幾乎沒差。
  - 重力方向：1.5 秒後倒。
  - 關節角：0.36 秒就倒。
  - 關節速度：不倒，但**超速 2–3 倍**（要 0.3 得 0.59，要 0.8 得 2.52）。
  - 上一次動作：站著不動，完全不走。
  - 這一組最有教學價值：同一個網路，五種感覺的重要性天差地別。
- **觀測延遲**：每多 12 ms 慢一點（0.47 → 72 ms 時 0.26 m/s），到 72 ms 都沒倒。
- **感測雜訊**：±0.2 以下沒事，±0.4 走不太動，±0.8 立刻倒。
- **PD 增益**：Kp 10 站不起來（機身 0.20 m），Kp 100 反而走更快（0.8 m/s）；Kd 小走得快，Kd 大走得慢。policy 是在 Kp 30 / Kd 1 下訓練的，這是 sim-to-real 的好例子。
- **地板摩擦**：μ 從 1 降到 0.05 幾乎沒影響（0.47 → 0.44）。後來驗證過了（見下方「摩擦的結果是真的」），結果成立。

## 還沒做的

- **沒有在瀏覽器裡跑過。** Node 和瀏覽器用的是同一個 WASM，速度應該接近，但手機、記憶體、以及 Next.js 16 怎麼打包這個 ESM + wasm 都沒試。這一步要動 `package.json`，會和主 session 撞，等它那邊告一段落再做。
- 渲染沒碰：官方 demo 用 Three.js。專案現在沒有 3D 函式庫，要選 Three.js 還是自己用 canvas 畫線框，是一個要決定的事。
- 網格減面沒做。
- 只有平地。樓梯地形在 deploy repo 裡有，但這個 policy 是不是為樓梯訓練的沒有查。

## 授權

`Lite3_rl_deploy`（policy）與 `deep_robotics_model`（MJCF、STL）都是 BSD-3-Clause：可以再散布，要保留版權聲明與授權全文。MuJoCo 是 Apache-2.0。文章頁尾要列出這三個來源。

## 給文章的初步骨架

1. 開場儀器：機器狗在走，讀者用搖桿給速度指令，旁邊即時顯示 45 個輸入和 12 個輸出。
2. 它沒有在這裡訓練：誠實說明訓練是在 Isaac Lab 用 GPU 做的，這裡只執行。這篇和前幾篇「在瀏覽器裡訓練」不同，要在開頭講清楚。
3. policy 就是三層矩陣乘法：把 19 萬個參數的 MLP 攤開，用 `lib/ml` 跑。
4. 它感覺得到什麼：45 維觀測逐組解釋，接「蒙住一種感覺」的互動。
5. 它不直接出力：動作 → 目標角度 → PD → 力矩，接 Kp/Kd 互動。
6. 推它：側推門檻。
7. 模擬和真機的差距：延遲、雜訊、增益不對會怎樣（都是量到的）。
8. 這和真的訓練差在哪：reward、domain randomization、teacher–student，留給之後有 GPU 的那一篇。

## 補充：摩擦的結果是真的

`lite3-spike/friction.py.txt`（Python MuJoCo）。接觸點實際用到的摩擦係數確實是設定值；腳掌著地時的水平滑動速度隨 μ 下降而上升，但 policy 撐得住：

| μ | 實際 vx（指令 0.5） | 著地腳的平均滑動速度 |
| --- | --- | --- |
| 1 | 0.47 | 0.12 m/s |
| 0.3 | 0.47 | 0.17 m/s |
| 0.1 | 0.46 | 0.22 m/s |
| 0.05 | 0.44 | 0.27 m/s |
| 0.01 | 0.24 | 0.47 m/s |

μ 0.1 再加 100 N 側推也沒倒；指令 1.5 m/s 時 μ 1 走 1.56、μ 0.1 走 1.31。合理的解釋是訓練時有做摩擦的 domain randomization（沒有查訓練設定，這是推測）。當互動的戲劇性不高，但可以配上「腳在滑」的讀數，拿來講 policy 的穩健性。

## 補充：瀏覽器 spike（分支 `feat/lite3`）

在獨立 worktree 做，草稿文章 `content/posts/lite3-walking`。headless Chromium（軟體 WebGL），桌機 1440 寬與 iPhone 13 尺寸、深淺兩個主題都看過截圖。

- 點「載入模擬器」之前不抓任何模擬器檔案。點了之後總傳輸 **4.51 MB**：wasm 2.52、policy 0.76、three.js 與 STLLoader 約 0.7（dev 模式未壓縮，production 會更小）、外觀網格 0.6、碰撞凸包 0.19。
- 碰撞網格換成凸包：3.4 MB → 189 KB，10 秒終點 4.70 m（原本 4.69）。外觀網格另外減面到 1.2 萬個三角形、600 KB。
- 模擬每個畫面 0.6–1.3 ms。fps 在 headless 的軟體 WebGL 下是 35–60，不代表真機，要在真的手機上量。
- Turbopack 要加 `resolveAlias: { module: { browser: "./lib/shims/empty.ts" } }`：Emscripten 載入器的 Node 分支有 `import("module")`。
- Three.js 的場景用 z-up（和 MuJoCo 一致），每個 body 直接套 `xpos`／`xquat`（注意 MuJoCo 的四元數是 w 在前）。右側大腿網格照 MJCF 鏡射，材質要改 `BackSide`。WebGL 的線永遠 1 px，地板格線改用貼圖。
- 推 250 N 會倒、四腳朝天也畫得對。
